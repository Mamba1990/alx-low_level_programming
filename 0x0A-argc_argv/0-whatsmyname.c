#include <stdio.h>
/**
 * main - Entry point.
 * @argc: argument count
 * @argv: argument vector (one dim array)
 *
 * Return: Always 0 (Success)
 */
int main (int argc, char *argv[])
{
	argc = 0;

	printf ("\"%s\".\n",argv[argc]);
return (0);
}
