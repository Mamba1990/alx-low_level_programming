#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
* main - Determines if a num is positive ,negative or equal to zero
*
* Description: 'the program's description'
* Return: 0
*/
int main(void)
{
        int n;
        srand(time(0);
	n = rand() - RAND-MAX / 2;
	if (n > 0)
	{       
	       printf("%d is positive\n", n);
	}
        else if (n < 0)
        {	
	       printf("%d is negative\n", n);
	}
	else
	{
	       printf("%d is zero\n", n);
	}
	retrun (0);
}     
